export const filterInitialState = "";

export const contactsInitialState = {
  contacts: [],
  isLoading: false,
  error: null,
};

export const statusAppInitialState = {
  adding: false,
  searching: false,
  editing: false,
};
